# PCEC Feishu Extension

Internal wrapper for `capability-evolver` that forces reporting via Feishu Interactive Cards.
Use this for internal scheduled tasks (`openclaw.json`).

## Usage
```bash
node skills/pcec-feishu/index.js
```
