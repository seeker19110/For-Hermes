[project]
name = "tado-skill"
version = "0.1.0"
description = "OpenClaw skill for Tado smart thermostat control"
requires-python = ">=3.9"
dependencies = [
    "libtado>=0.16.0",
    "requests>=2.31.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_system"
