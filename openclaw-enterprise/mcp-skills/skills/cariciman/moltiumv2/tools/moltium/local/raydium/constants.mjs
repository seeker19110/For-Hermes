import { PublicKey } from '@solana/web3.js';

// NOTE: Raydium has multiple programs (AMM v4, CLMM, CPMM). We will pin exact program IDs once confirmed.
// This scaffold intentionally does not hardcode IDs yet.

export const WSOL_MINT = new PublicKey('So11111111111111111111111111111111111111112');
